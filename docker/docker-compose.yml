version: '3.8'

services:
  test-runner:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: test-automation-framework
    environment:
      - HEADLESS=true
    volumes:
      - ../reports:/app/reports
    command: ["pytest"]
